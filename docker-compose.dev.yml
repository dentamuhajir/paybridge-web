version: "3.9"

services:
  paybridge-web:
    build:
      context: .
      target: dev
    container_name: paybridge-web-dev
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - .:/app
      - /app/node_modules
