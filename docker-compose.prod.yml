version: "3.9"

services:
  paybridge-web:
    image: dentamuhajir/paybridge-web:1.0.0
    build:
      context: .
      target: prod
    container_name: paybridge-web
    ports:
      - "80:80"
    restart: always
    environment:
      - NODE_ENV=production
